

Bölüm 9 VİDEO 82 Foreach Döngüsü


Temel Konular - Döngüler

Döngüler belirli bir sayıda yapan veya belirli bir şarta göre yapan yapılardır.

For - Foreach - While - do While olarak isimlendirdiğimiz döngülerimizi davranış tiplerine göre ayıralım.

Sabit Döngü tipleri

For - Foreach

Şartlı Döngü tipleri

While - do While

---------------------------

bu bölümde foreach döngüsünü göreceğiz

yapısını davranışını ve ne için kullanıldığını inceliyor olacağız

detaylı ve ödevli bölümleri diziler konusunun içerisinde yapmak zorundayım

çünkü foreach aslında çalışırken sabit degerini mevcut olan bir veri kümesinin içerisinde

bulunan adetten almakta

foreach i anlatmak için diziler konusundan yardım alacagız

sonra ileride diziler konusunu daha kolay anlacaksınız

program.cs e gecelim

önce bir dizi olusturalım

string[] isimler = new[] { "Mücahit","Muhammet","Çetinkaya","Arden","Arthur","Zayn" };

elimizde bir veri bütünü var bizim bu veri bütünü üzerinde işlem yapmamız gerekiyor

standart kullanımda for foreach bizim cok sık kullandıgımız döngülerden bir tanesi

daha az kullandıklarımız do while mesela

bir veri kaynagından database den bütün bir datayı cektiginizi dusunun

bu veri kaynagında operasyonel işlemleriniz var ne gibi

elinizde bir insert var database e kayıt etmek istiyorsunuz

veri kümesi düşünelim içerisinde

müşterilerin ıd leri ve bilgileri var

ilk once verileri database den kontrol etmeniz gerekecek

ilgili müşterinin id si benim sistemim de var mı

ilgili müşterinin tknc vs bilgileri benim sistemim de var mı

id den bulamıyorsam sosyal hesabından bulmam gerekecek

bu bilgilere ait bir müşteri bende var mı diye dönmek istediğimde

o veri kümesi içinde dönmek isteyeceğiz

işte o zaman burada foreach döngüsünü kullanıyor olacagız

aynı işlem for içinde yapılabilir ama bazı şeylerin standartı oluyor

standart olan da eger bir tip listesi içinde dönüyosan

bunu foreach ile yapmaktır.

yazımı

foreach tab tab yapınca geliyor

foreach (var item in collection)

{

}

foreach (string item in isimler)

{

}

isimler dizisinin içinde dön hepsine item adını ver demiş oluyoruz

item degisebilir

dizi string dizisi oldugundan foreach te string acıldı

string[] isimler = new[] { "Mücahit","Muhammet","Çetinkaya","Arden","Arthur","Zayn" };

foreach (string item in isimler)

{

Console.WriteLine(item);

}

Console.ReadLine();

yazdıgımızda dizi içinde döndügü için kaç tane verisi varsa o kadar dönüyor

her veri de bir tane okuyor

breakpoint ekleyip bakınca

isimler veri kümesi dizisi 6 elamanlı bir dizi index i 5 oluyor sebebi index 0 dan baslıyor

basta dizi içine giriyor kac index var 5 o kadar dönüyor içinde foreach ile sıra sıra yazdırıyor hepsini

foreach e girdiğinde 0. index i okuyor

bunu item in uzerıne alıyor

item i da yazdırıyoruz burda

hangisini okursa onu item olarak kabul ediyor

en basta zaten diziyi okudu isimler dizisinin içinde döndüğümüz için

ordan bakıyor

diziler konusunun içerisine girdiğimiz de

dizi örneklerinde foreach i cok sık kullanacagız

suan bilmeniz gereken foreach in koleksiyon içerisinde döngüsel bir işleme girdiğimizde

kullanacagımız bir döngü oldugunu

item in koleksiyon içerisindeki tek bir degeri işaret ettiğini bilmemiz yeterli

string[] isimler = new[] { "Mücahit","Muhammet","Çetinkaya","Arden","Arthur","Zayn" };

foreach (string item in isimler)

{

Console.WriteLine(item);

}

Console.ReadLine();