
 
Bölüm 9 VİDEO 84 While döngüsü - Örnek uygulama serisi - ( 2 farklı örnek uygulama içerir )

/*

Çalışma 1
Kullanıcıdan gelen sayısal değere göre ekrana 1 eksilterek 0 değerine ulaşıncaya kadar ekrana yazdıralım
mesela ekrana 3 yazdık
3 2 1 0 olacak
 
*/

/*
Console.Write("Sayı giriniz : ");
int kullaniciGelen = Convert.ToInt32(Console.ReadLine());

while (kullaniciGelen != -1)
{
    Console.WriteLine(kullaniciGelen);
    kullaniciGelen--;
}
*/

/*--------------------------------------------------------------------------------------------*/

/*
 
Çalışma 2
Kullanıcıdan gelen değerlerin toplamını ekrana yazdıralım.
0 dan küçük bir değer girişi yapılırsa yani -1 mesela
toplama işlemini bitirelim ve ekrana toplam sonucu yazalım


önce kullanıcıyı bilgilendirelim
Console.Write(" Lütfen toplamak istediğiniz sayıları giriniz : ");
Console.Write(" Not 0 dan küçük bir değer işlemleri durdurur. ");

kullanıcıdan bir deger almam gerekiyor ama bu degeri döngü içinde almam lazım
bu döngü içinden gelen degeri de bir sarta sokmam gerekiyor
sart dogru oldugu sürece işleme devam edecek yanlıs oldugunda duracak


yapmak istediğim işlem toplama işlemi
dısarıda bir degisken olusturalım
int toplamDeger = 0;
while dongumuzu yazalım
burda sonsuz dönguyu kullanacagız burda devamlı olarak true donecek ama break ile bunu kıracagız

toplamDeger 0 while içine girecek
kullanıcıdan deger istedi
string geleni int e cevirdi
girilen deger 0 dan kucuk mu bakıyor degilse else ye girdi
gelen deger ile toplam degeri topladı toplam degerin ustune yazdı
ilk toplam deger sıfırdı
0 dan kucuk girildiğinde if kısmına gelir break ile kırılır

*/

Console.WriteLine(" Lütfen toplamak istediğiniz sayıları giriniz : ");
Console.WriteLine(" Not -1 değeri işlemleri durdurur. ");

int toplamDeger = 0;

while (true)
{
    string kullaniciGelenDegerString = Console.ReadLine();
    int kullaniciGelenDegerInt = Convert.ToInt32(kullaniciGelenDegerString);
    if (kullaniciGelenDegerInt < 0)
    {
        break;
    }
    else
    {
        toplamDeger = toplamDeger + kullaniciGelenDegerInt;
    }
}

Console.WriteLine("Toplam Değer : {0}" , toplamDeger);

Console.ReadLine();